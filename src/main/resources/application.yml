# application.yml

 
 spring:
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: idongesitukut25@gmail.com
    password: mariner25
    port: 587
    properties:
      mail:
        smtp:
          port: 25
          auth: true
          starttls:
              enable: true
          socketFactory:
            port: 587
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
    protocol: smtp
    test-connection: false